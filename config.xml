<?xml version="1.0" encoding="UTF-8" ?>
<widget xmlns="http://www.w3.org/ns/widgets" xmlns:gap="http://phonegap.com/ns/1.0" id="com.Encounter.MemorizeMe" version="0.2.0">
	<name>Memorize Me Beta 1</name>
	<description>A digital scripture memory system</description>
	<author href="http://www.franklockerman.net/memorizeme">Frank Lockerman</author>

	<!-- Preferences -->
	
	<preference name="phonegap-version" value="3.4.0" />
	<preference name="target-device" value="universal" />
	<preference name="fullscreen" value="true" />
	<preference name="webviewbounce" value="true" />
	<!-- ios: external links should open in the default browser, 'true' would use the webview the app lives in -->
	<preference name="stay-in-webview" value="false" />
	<preference name="exit-on-suspend" value="false" />
	<!-- android: app install location. 'auto' will choose. 'internalOnly' is device memory. 'preferExternal' is SDCard. -->
	<preference name="android-installLocation" value="auto" />

	<!-- Plugins -->
	
	<gap:plugin name="org.apache.cordova.media" />

	<!-- Configurations -->
	
	<gap:config-file platform="ios" parent="UIViewControllerBasedStatusBarAppearance" overwrite="true">
		<false />
	</gap:config-file>
	<gap:config-file platform="ios" parent="UIStatuseBarHidden" overwrite="true">
		<true />
	</gap:config-file>

	<!-- Access to external domains -->
	
	<access origin="*" />
	<!--
        <access origin="http://phonegap.com" />                    - allow any secure requests to http://phonegap.com/
        <access origin="http://phonegap.com" subdomains="true" />  - same as above, but including subdomains, such as http://build.phonegap.com/
        <access origin="http://phonegap.com" browserOnly="true" /> - only allows http://phonegap.com to be opened by the child browser.
    -->
</widget>

